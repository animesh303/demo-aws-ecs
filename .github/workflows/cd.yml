name: CD Pipeline

on:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  infrastructure:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    outputs:
      ecr_registry: ${{ steps.ecr-details.outputs.registry }}
      ecr_repository: ${{ steps.ecr-details.outputs.repository }}
      ecs_cluster: ${{ steps.ecs-details.outputs.cluster }}
      ecs_service: ${{ steps.ecs-details.outputs.service }}
    steps:
      - uses: actions/checkout@v3
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Init
        working-directory: terraform
        run: terraform init

      - name: Terraform Plan
        working-directory: terraform
        run: terraform plan

      - name: Terraform Apply
        id: terraform-apply
        working-directory: terraform
        run: terraform apply -auto-approve

      - name: Get ECR Details
        id: ecr-details
        working-directory: terraform
        run: |
          if [ "${{ steps.terraform-apply.outcome }}" != "success" ]; then
            echo "Error: Terraform apply failed"
            exit 1
          fi
          
          REPO_URL=$(terraform output -raw ecr_repository_url 2>&1)
          if [[ "$REPO_URL" == *".dkr.ecr."* ]]; then
            REGISTRY=$(echo "$REPO_URL" | sed 's|/.*||')
            REPOSITORY=$(echo "$REPO_URL" | sed 's|.*/||')
          else
            REGISTRY="${{ vars.ECR_REGISTRY }}"
            REPOSITORY="${{ vars.ECR_REPOSITORY }}"
          fi
          
          echo "registry=$REGISTRY" >> $GITHUB_OUTPUT
          echo "repository=$REPOSITORY" >> $GITHUB_OUTPUT

      - name: Get ECS Details
        id: ecs-details
        working-directory: terraform
        run: |
          CLUSTER=$(terraform output -raw ecs_cluster_name 2>&1 || echo "${{ vars.ECS_CLUSTER }}")
          SERVICE=$(terraform output -raw ecs_service_name 2>&1 || echo "${{ vars.ECS_SERVICE }}")
          
          if [[ -z "$CLUSTER" ]] || [[ -z "$SERVICE" ]]; then
            echo "Error: ECS values not found"
            exit 1
          fi
          
          echo "cluster=$CLUSTER" >> $GITHUB_OUTPUT
          echo "service=$SERVICE" >> $GITHUB_OUTPUT

  build:
    needs: [infrastructure]
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and Push Image
        env:
          ECR_REGISTRY: ${{ needs.infrastructure.outputs.ecr_registry }}
          ECR_REPOSITORY: ${{ needs.infrastructure.outputs.ecr_repository }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          cd app
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

  deploy:
    needs: [infrastructure, build]
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Set ECS Values
        run: |
          ECS_CLUSTER="${{ needs.infrastructure.outputs.ecs_cluster }}"
          ECS_SERVICE="${{ needs.infrastructure.outputs.ecs_service }}"
          if [[ -z "$ECS_CLUSTER" ]] || [[ -z "$ECS_SERVICE" ]]; then
            echo "Error: ECS values not set"
            exit 1
          fi
          echo "ECS_CLUSTER=$ECS_CLUSTER" >> $GITHUB_ENV
          echo "ECS_SERVICE=$ECS_SERVICE" >> $GITHUB_ENV

      - name: Deploy to ECS
        run: aws ecs update-service --cluster $ECS_CLUSTER --service $ECS_SERVICE --force-new-deployment

      - name: Wait for Service Stability
        continue-on-error: true
        run: aws ecs wait services-stable --cluster $ECS_CLUSTER --services $ECS_SERVICE